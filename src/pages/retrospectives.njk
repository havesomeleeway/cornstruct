---
layout: post
title: Retrospectives
description: 'Long-form reflections and retrospectives on projects, experiences, and learnings.'
pagination:
  data: collections.retrospectives
  size: 5
permalink: 'retrospectives/{% if pagination.pageNumber >=1 %}page-{{ pagination.pageNumber + 1 }}/{% endif %}index.html'
---

<div class="wrapper">
  <div class="region flow prose">
    <p>Long-form reflections and retrospectives on projects, experiences, and learnings.</p>
    {% asyncEach item in pagination.items %}
      {% include "partials/card-blog.njk" %}
    {% endeach %}
  </div>

  <!-- set collection to paginate -->
  {% set collectionToPaginate = collections.retrospectives %}
  <!-- set target pagination settings in meta.js -->
  {% set metaKey = "retrospectives" %}
  <!-- if the number of items in the collection is greater than the number of items shown on one page -->
  {% if collectionToPaginate.length > pagination.size %}
    <!-- include pagination -->
    {% include 'partials/pagination.njk' %}
  {% endif %}
</div> 